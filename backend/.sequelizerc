const path = require('path');

module.exports = {
  'config': path.resolve('dist', 'sequelize.js'),
  'models-path': path.resolve('dist', 'model'),
  'migrations-path': path.resolve('dist', 'migration'),
}
